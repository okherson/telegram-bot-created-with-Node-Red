module.exports = function(RED) {
    function check_time(config) {
        RED.nodes.createNode(this,config);
        var node = this;
        node.checkField = config.checkField
        node.checkFieldType = config.checkFieldType
        node.log(JSON.stringify(config));
        node.on('input', function(msg) {
                const re = /^(([0-1]?[0-9])|([2][0-3])):([0-5]?[0-9])(:([0-5]?[0-9]))?$/;
                if(re.test(String(msg.topic))){
                    node.send([msg,])
                }else{
                    node.send([,msg])
                }
                
            
        });
    }
    RED.nodes.registerType("check_time",check_time);
}