module.exports = async function (templates) {
  try {
    const templateNamesList = [];

    await templates.forEach(template => {
      templateNamesList.push(template.templateId);
    });

    return templateNamesList;
  } catch
    (error) {
    throw new Error(error.message);
  }
};
